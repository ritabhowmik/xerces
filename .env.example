# Optional. Without this, /api/quote returns a deterministic mock price
# per symbol so the app works with zero configuration.
#
# Get a free key at https://finnhub.io/register (free tier covers this app's
# quote-lookup volume comfortably).
FINNHUB_API_KEY=
